version: '3.8'

services:
  youtube-tools-api:
    build: .
    container_name: youtube_tools_api
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLASK_ENV=production
    restart: always
